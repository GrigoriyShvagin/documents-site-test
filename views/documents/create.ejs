<!DOCTYPE html>
<html lang="ru">
  <head>
    <%- include('../partials/head', { title }) %>
  </head>
  <body>
    <%- include('../partials/header') %>

    <main>
      <div class="container">
        <h1>Добавить документ</h1>

        <% if (error) { %>
        <div class="alert alert-error"><%= error %></div>
        <% } %>

        <form
          method="POST"
          action="/documents/create"
          class="document-form"
          enctype="multipart/form-data"
        >
          <div class="form-group">
            <label for="title">Название документа*</label>
            <input type="text" id="title" name="title" required />
          </div>

          <div class="form-group">
            <label for="description">Описание</label>
            <textarea id="description" name="description" rows="3"></textarea>
          </div>

          <div class="form-group">
            <label for="document_file">Загрузить файл</label>
            <input
              type="file"
              id="document_file"
              name="document_file"
              accept=".pdf,.doc,.docx"
            />
            <small>Разрешенные форматы: PDF, DOC, DOCX (макс. 10MB)</small>
          </div>

          <div class="form-group">
            <label for="content">Альтернативное текстовое содержимое</label>
            <textarea
              id="content"
              name="content"
              rows="10"
              placeholder="Введите текст документа (если нет файла)..."
            ></textarea>
            <small>Заполняется, если нет загружаемого файла</small>
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-primary">
              Создать документ
            </button>
            <a href="/documents" class="btn btn-outline">Отмена</a>
          </div>
        </form>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
