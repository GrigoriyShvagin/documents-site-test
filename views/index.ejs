<!DOCTYPE html>
<html lang="ru">
  <head>
    <%- include('partials/head') %>
  </head>
  <body>
    <%- include('partials/header') %>

    <main>
      <section class="hero">
        <div class="container">
          <h2>DocHub — современная облачная библиотека документов</h2>
          <p>
            Электронная библиотека для организации информации, обучения
            сотрудников и хранения важных документов в едином защищенном
            пространстве
          </p>
          <div class="mt-2">
            <% if (user) { %>
            <a href="/documents" class="btn">Перейти к документам</a>
            <% } else { %>
            <a href="/auth/login" class="btn">Войти в систему</a>
            <a href="/auth/register" class="btn btn-secondary">Регистрация</a>
            <% } %>
          </div>
        </div>
      </section>

      <section class="container">
        <h3 class="section-title">Возможности системы</h3>
        <div class="features">
          <div class="feature-card">
            <h3>🔍 Гибкий поиск</h3>
            <p>Быстро находите нужные документы по названию или содержанию</p>
          </div>
          <div class="feature-card">
            <h3>👥 Управление ролями</h3>
            <p>
              Разграничение доступа для пользователей, администраторов и службы
              поддержки
            </p>
          </div>
          <div class="feature-card">
            <h3>📞 Поддержка</h3>
            <p>
              Встроенная система обратной связи для быстрого решения вопросов
            </p>
          </div>
        </div>
      </section>

      <% if (recentDocuments && recentDocuments.length > 0) { %>
      <section class="container">
        <h3 class="section-title">Последние добавленные документы</h3>

        <% recentDocuments.forEach(function(document) { %>
        <div class="document-card">
          <div class="document-card-flex">
            <div>
              <h3><%= document.title %></h3>
              <p><%= document.description %></p>
              <p class="document-card-date">
                Автор: <%= document.username || 'Неизвестно' %> | <%= new
                Date(document.created_at).toLocaleDateString('ru-RU') %>
              </p>
            </div>
            <div class="button-group">
              <% if (user) { %>
              <a href="/documents/<%= document.id %>" class="btn"
                >Просмотреть</a
              >
              <% } else { %>
              <a href="/auth/login" class="btn">Войти для просмотра</a>
              <% } %>
            </div>
          </div>
        </div>
        <% }); %> <% if (user) { %>
        <div class="mt-2" style="text-align: center">
          <a href="/documents" class="btn">Посмотреть все документы</a>
        </div>
        <% } %>
      </section>
      <% } %>
    </main>

    <%- include('partials/footer') %>

    <script src="/js/index.js"></script>
  </body>
</html>
